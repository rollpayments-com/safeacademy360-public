// Shared constants. Prices are stated here once and rendered everywhere they appear in scripts.
export const PRICE_ACADEMY_MONTHLY = 25;
export const PRICE_COACH_MONTHLY = 5;
export const SITE_URL = "https://safeacademy360.com";
export const money = (n) => "$" + n.toLocaleString("en-US", { maximumFractionDigits: 0 });
