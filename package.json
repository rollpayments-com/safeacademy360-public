{
  "name": "safeacademy360-site",
  "version": "0.1.0",
  "private": true,
  "description": "Public website for Safe Academy 360. Static, zero dependency.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch --serve --fixtures",
    "check": "node scripts/build.mjs --check"
  }
}
